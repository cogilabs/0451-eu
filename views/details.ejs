<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%- frontmatter.title %> - Details</title>
  <link href="/output.css" rel="stylesheet">
</head>
<body class="bg-gray-100 text-gray-800 flex flex-col min-h-screen m-0">
  <header class="bg-blue-500 text-white p-4">
    <h1 class="text-2xl font-bold">0451.eu - Easter Eggs Archive</h1>
    <nav class="flex space-x-4">
      <a href="/" class="hover:text-blue-300">Home</a>
      <a href="/about" class="hover:text-blue-300">About</a>
    </nav>
  </header>
  <main class="container mx-auto p-6 flex-1">
    <div class="max-w-4xl mx-auto">
      <!-- Hero Section -->
      <div class="relative h-96 rounded-xl overflow-hidden mb-8">
        <img src="<%= frontmatter.image %>" alt="<%= frontmatter.title %>" class="w-full h-full object-cover">
        <div class="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-black/70 to-transparent p-6">
          <h1 class="text-4xl font-bold text-white mb-2"><%- frontmatter.title %></h1>
          <div class="flex gap-2">
            <% frontmatter.tags.forEach(tag => { %>
              <span class="px-3 py-1 text-sm font-bold text-white rounded-full border-2 bg-<%= tag.color %> bg-opacity-50 border-<%= tag.color %>">
                <%= tag.name %>
              </span>
            <% }); %>
          </div>
        </div>
      </div>

      <!-- Content Section -->
      <div class="bg-white rounded-xl shadow-lg p-8">
        <article class="prose prose-slate lg:prose-lg xl:prose-xl mx-auto">
          <%- content %>
        </article>
      </div>
    </div>
  </main>
  <footer class="bg-gray-800 text-white p-4 text-center mt-8">
    <p>&copy; 2025 <a href="https://Cogilabs.eu/" class="hover:text-blue-300">Cogilabs.eu</a></p>
  </footer>
</body>
</html>
